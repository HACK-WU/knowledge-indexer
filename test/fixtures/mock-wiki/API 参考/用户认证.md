# 用户认证

本文档描述系统的用户认证机制。

## 认证流程

1. 客户端发送用户名 + 密码
2. 服务端校验凭据
3. 签发 JWT Token
4. 后续请求携带 Token 进行鉴权

## Token 刷新

Token 有效期 2 小时，过期前 30 分钟内可使用 Refresh Token 续期。

## 安全要求

- 密码必须 bcrypt 加密存储
- Token 使用 RS256 签名
- 敏感接口强制二次验证
